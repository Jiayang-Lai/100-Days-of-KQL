# Day 18

new_guid() scalar function generates a GUID.

# Official Document

[new_guid()](https://learn.microsoft.com/en-us/kusto/query/new-guid-function?view=microsoft-fabric)

# Note

This function generates a non-deterministic GUID every time, would be nice if the function supports an argument as seed.
